services:
  server:
    container_name: "engine-service"
    build:
      context: .
    ports:
      - "8081:8081"
    env_file:
      - .env
    depends_on:
      - db

  db:
    container_name: engine-postgres-db
    image: postgres:17.6
    ports:
      - "${DB_PORT}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data: